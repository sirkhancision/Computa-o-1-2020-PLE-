#include <stdio.h>

int main(){
    int valor_n;
    int pares = 2;
    int potencia = pares * pares;
    scanf("%d", &valor_n);
    do {
        printf("%d\n", pares);
    } while (pares + 2, pares);
    
    return 0;
}