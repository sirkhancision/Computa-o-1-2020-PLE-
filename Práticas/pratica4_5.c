#include <stdio.h>

int main(){
    int numero, divisor = 1;;
    scanf("%d", &numero);

    while (numero <= 1, divisor <= numero){
        if (typeof(divisor) == int){
        printf("%d\n", divisor);
        numero--, divisor++;
        }
    }

    return 0;
}